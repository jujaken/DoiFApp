<Window x:Class="DoiFApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DoiFApp.Views"
        xmlns:vm="clr-namespace:DoiFApp.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Grid>

        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>

        </Grid.ColumnDefinitions>

        <Grid x:Name="ToolsZone" Grid.Column="0">

            <ListBox Background="Gray"
                     ItemsSource="{Binding Tools}">

                <ListBox.ItemTemplate>
                    
                    <DataTemplate>

                        <local:ToolView DataContext="{Binding}"/>

                    </DataTemplate>

                </ListBox.ItemTemplate>

            </ListBox>

        </Grid>

        <Grid x:Name="PageZone" Grid.Column="1">

            <Border Background="DarkGray"/>
            <ContentControl Content="{Binding CurPage}"/>

        </Grid>

        <Grid x:Name="NotifiesZone" Grid.Column="2">

            <ListBox Background="Gray" ItemsSource="{Binding Notifies}">

                <ListBox.ItemTemplate>

                    <DataTemplate>

                        <local:NotifyView DataContext="{Binding}"/>

                    </DataTemplate>

                </ListBox.ItemTemplate>

            </ListBox>

        </Grid>

    </Grid>
    
</Window>
