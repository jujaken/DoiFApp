<UserControl x:Class="DoiFApp.Views.Pages.AppSettingsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DoiFApp.Views.Pages"
             xmlns:vm="clr-namespace:DoiFApp.ViewModels.Pages"
             xmlns:сontrolls="clr-namespace:DoiFApp.Views.Controlls"
             mc:Ignorable="d" 
             
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:AppSettingsPageViewModel, IsDesignTimeCreatable=True}">

    <Grid Height="{Binding ActualHeight,
                  RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=Window}}">

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="64"/>
        </Grid.RowDefinitions>

        <ScrollViewer>

            <ItemsControl ItemsSource="{Binding ConfigColorCategories}">

                <ItemsControl.ItemTemplate>

                    <DataTemplate>

                        <Grid>

                            <StackPanel>

                                <Grid>
                                    <Grid.Background>
                                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0,0">
                                            <GradientStop Color="#72AE159F"/>
                                            <GradientStop Color="#00B56C20" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Grid.Background>
                                    <TextBlock Text="{Binding Tittle}" Margin="5" FontSize="20" />
                                </Grid>
                              
                                <ItemsControl ItemsSource="{Binding Colors}">

                                    <ItemsControl.ItemTemplate>

                                        <DataTemplate>

                                            <сontrolls:AppSettingsPage DataContext="{Binding}" Margin="5"/>

                                        </DataTemplate>

                                    </ItemsControl.ItemTemplate>

                                </ItemsControl>


                            </StackPanel>

                        </Grid>

                    </DataTemplate>

                </ItemsControl.ItemTemplate>

            </ItemsControl>

        </ScrollViewer>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Margin="5">

            <Button Style="{StaticResource MainButton}" Content="Установить по умолчанию" Command="{Binding SetDefaultCommand}" Margin="0,0,5,0"/>
            <Button Style="{StaticResource MainButton}" Content="Сохранить" Command="{Binding SaveCommand}" />

        </StackPanel>

    </Grid>
</UserControl>
